apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
data:
  default.conf: |
    server {
        listen 80;
        server_name rgz.local;
        location / {
            proxy_pass http://flask-app:5000;
        }
    }
